# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Local tool artifacts (not shared/tracked)
.claude/
.cursor/skills/
**/__pycache__/

# Supabase CLI local state, written by `supabase link` — machine-specific
# (project ref, pooler URL, remote version pins) and regenerated on demand.
# supabase/config.toml is the shared, tracked config; this directory is not.
supabase/.temp/